{
  "name": "elastic-security-rule-disco",
  "version": "1.0.0",
  "description": "Simple platform for discovering Elastic Security rules based on integrations",
  "private": true,
  "scripts": {
    "dev": "docker-compose up --build",
    "dev:logs": "docker-compose logs -f",
    "down": "docker-compose down",
    "clean": "docker-compose down -v && docker system prune -f",
    "setup": "cp .env.example .env || echo 'Create .env file with your Elastic credentials'"
  },
  "devDependencies": {
    "typescript": "^5.4.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "keywords": [
    "elasticsearch",
    "security",
    "detection-rules",
    "docker"
  ],
  "author": "Marcus Jeffes",
  "license": "MIT"
}